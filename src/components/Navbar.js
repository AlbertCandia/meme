import React from 'react';
import '../stylesheets/navbar.css';

const Navbar = () => {
  return (
    <nav className="teal">
      <p className="brand-logo">Generador de Memes CaC.</p>
    </nav>
  );
};

export default Navbar;
